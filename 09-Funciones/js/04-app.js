// Métodos. Igual que funciones pero varía en función de dónde se ejecuta o su contexto.
const numero1 = 20;
const numero2 = '20';

console.log(parseInt(numero2)); // Esto es una función. función(variable)

console.log(numero1.toString()); // Esto es un método. variable.método()
