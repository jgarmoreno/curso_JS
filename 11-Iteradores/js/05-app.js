// Do While
let i = 0; // Inicio

do {
    console.log(`Número ${i}`)
    i++; // Incremento
} while (i < 10); // Condición

// La diferencia es que al menos, se ejecuta una vez y después comprueba la condición.