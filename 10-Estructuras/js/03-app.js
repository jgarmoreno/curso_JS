// Operador mayor que y menor que
const dinero = 500;

const totalAPagar = 300;

if(totalAPagar <= dinero) {
    console.log('Tienes dinero para pagar');
} else {
    console.log('No puedes pagar');
}